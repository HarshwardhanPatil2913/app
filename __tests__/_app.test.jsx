import { render, screen } from '@testing-library/react';
import MyApp from '../pages/_app';

describe('MyApp', () => {
  test('renders MypagePage', () => {
    // eslint-disable-next-line testing-library/no-debugging-utils
    screen.debug();
  });
});
